<h1>Edit Question</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guildlines for questions</p>
    <ul>
      <li>Check your spelling.</li>
      <li>Make sure to make your question clear to get better responses.</li>
      <li>Use correct grammar.</li>
    </ul>
  </div>

  <div class="col-md-8">
    <%= form_for @question do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter post title" %>
      </div>
      <div class="form-group">
        <%= f.label :resolved%>
        <%= f.check_box :resolved, class: 'form-control' %>
      </div>
      <div class="col-md-8">
        <%= f.label :body%>
        <%= f.text_area :body, class: 'form-control', placeholder: "Enter post body" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <%end%>
  </div>

</div>
