<h1>New Question</h1>

  <div class="row">
    <div class="col-md-4">
      <p>Guildlines for questions</p>
      <ul>
        <li>Check your spelling.</li>
        <li>Make sure to make your question clear to get better responses.</li>
        <li>Use correct grammar.</li>
      </ul>
    </div>

    <div class="col-md-8">
      <%= form_for @question do |f| %>
        <div class="form-group">
          <%= f.label :title %>
          <%= f.text_field :title, class: 'form-control', placeholder: 'Enter question title' %>
        </div>
        <div class="form-group">
          <%= f.label :body %>
          <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: 'Enter question body' %>
        </div>
        <div class="form-group">
          <%= f.submit "Save", class: "btn btn-success" %>
        </div>
        <!--when using ruby in html make sure to add the end code -->
        <%end%>
    </div>
  </div>
